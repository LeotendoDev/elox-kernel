name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make build-x86_64
    - name: qemu
      run: qemu-system-x86_64 dist/x86_64/kernel.iso
